namespace BallastLane.TaskManager.Common;

/// <summary>
/// Direction of a sort: ascending or descending.
/// </summary>
public enum SortOrder
{
    /// <summary>Ascending order (smallest or earliest first).</summary>
    Asc = 0,
    /// <summary>Descending order (largest or latest first).</summary>
    Desc = 1,
}
